#include<stdio.h>
int main()
{
    int a = 10;
    int*ptr;
    ptr = &a;
    printf("value of a:%d\n",a);
    printf("address of a:%p\n",&a);
    printf("value of ptr(adress stored):%p\n",ptr);
    printf("value pointed to by ptr: %d\n",ptr);
    return 0;
}
